/*
 * author: wenwenziy
 * title: 中缀转后缀并计算10
 * class: myStack
 */
#include <iostream>
#include <cstdio>
using namespace std;
const int MAX_LEN = 1e6;
char st[MAX_LEN], exp[MAX_LEN];
int top;

void infix2posfix (char *ch) {
	int len = strlen(ch);
}

void solveExp (char *ch) {
	
}

int main () {
	scanf("%s", exp);
	infix2posfix (exp);
	solveExp (exp);
	return 0;
}
